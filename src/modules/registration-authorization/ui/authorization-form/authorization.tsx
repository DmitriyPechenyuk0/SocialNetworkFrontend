import { authorizationSchema } from "../../model/schemas";
import { View } from "react-native";

export function Authorization() {
    return (
        <View className = "container">
            {/* Authorization form components go here */}
        </View>
    )
}