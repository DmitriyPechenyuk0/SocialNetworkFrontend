import { registrationSchema } from "../../model/schemas";
import { View } from "react-native";

export function Registration() {
    return (
        <View className = "container">
            {/* Registration form components go here */}
        </View>
    )
}